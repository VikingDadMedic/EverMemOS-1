"""
Identity Topology - Omega's persistent identity system.

Manages omega_scar.json: immutable invariants (topological holes)
and flexible regions (deformable surface). Validates changes,
detects drift, evolves within bounds.
"""
